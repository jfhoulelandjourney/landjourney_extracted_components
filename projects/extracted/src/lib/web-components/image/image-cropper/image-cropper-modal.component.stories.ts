// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { ImageCropperModalComponent } from './image-cropper-modal.component';

const meta: Meta<ImageCropperModalComponent> = {
  title: 'Web Components / Image / Image Cropper / Image Cropper Modal',
  component: ImageCropperModalComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<ImageCropperModalComponent>;

export const Default: Story = {
  args: {},
};
