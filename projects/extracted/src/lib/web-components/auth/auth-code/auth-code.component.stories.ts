// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { AuthCodeComponent } from './auth-code.component';

const meta: Meta<AuthCodeComponent> = {
  title: 'Web Components / Auth / Auth Code',
  component: AuthCodeComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<AuthCodeComponent>;

export const Default: Story = {
  args: {},
};
