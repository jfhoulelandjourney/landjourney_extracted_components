// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { PrintLayoutAdapterComponent } from './print-layout-adapter.component';

const meta: Meta<PrintLayoutAdapterComponent> = {
  title: 'Web Components / Layout / Print Layout Adapter',
  component: PrintLayoutAdapterComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<PrintLayoutAdapterComponent>;

export const Default: Story = {
  args: {},
};
