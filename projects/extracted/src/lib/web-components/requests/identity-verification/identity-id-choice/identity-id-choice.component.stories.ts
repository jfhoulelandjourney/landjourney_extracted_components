// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { IdentityIdChoiceComponent } from './identity-id-choice.component';

const meta: Meta<IdentityIdChoiceComponent> = {
  title: 'Web Components / Requests / Identity Verification / Identity Id Choice',
  component: IdentityIdChoiceComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<IdentityIdChoiceComponent>;

export const Default: Story = {
  args: {},
};
