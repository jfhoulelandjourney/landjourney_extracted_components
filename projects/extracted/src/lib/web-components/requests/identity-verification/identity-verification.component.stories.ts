// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { IdentityVerificationComponent } from './identity-verification.component';

const meta: Meta<IdentityVerificationComponent> = {
  title: 'Web Components / Requests / Identity Verification',
  component: IdentityVerificationComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<IdentityVerificationComponent>;

export const Default: Story = {
  args: {},
};
