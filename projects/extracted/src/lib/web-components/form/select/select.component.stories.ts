// AUTO-GENERATED STUB — replace with a real story when ready.
// See PLAN.md "Working patterns" for the full story template.
import type { Meta, StoryObj } from '@storybook/angular';
import { LjSelectComponent } from './select.component';

const meta: Meta<LjSelectComponent> = {
  title: 'Web Components / Form / Select',
  component: LjSelectComponent,
  tags: ['autodocs'],
  parameters: {
    docs: {
      description: {
        component:
          'Auto-generated stub. May render with errors until real mock inputs are provided. Replace with a hand-written story to fix.',
      },
    },
  },
};

export default meta;
type Story = StoryObj<LjSelectComponent>;

export const Default: Story = {
  // Attribute-only selector ('[lj-select]') — the component is meant to
  // be applied to a host element. A meaningful story needs a real host.
  args: {},
};
